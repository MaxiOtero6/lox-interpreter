[project]
name = "lox-interpreter"
version = "0.1.0"
description = "Lox language interpreter coded in python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "prompt-toolkit>=3.0.52",
    "pytest>=9.0.2",
]
